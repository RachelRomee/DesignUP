<h1> Updates from designers you follow </h1>


<% @posts.each do |post| %>
    <a href="<%= post_path post%>">
    <%= image_tag(post.image.thumbnail.url :class => "thumbnail inline") %> <br>
      <h2><%= post.title %></h2>
      <p><%= post.description %><p>
    </a>
<% end %>
